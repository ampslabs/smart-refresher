include: package:flutter_lints/flutter.yaml

analyzer:
  strong-mode:
    implicit-casts: false
    implicit-dynamic: false
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true

  errors:
    # Treat these as errors, not warnings
    missing_required_param: error
    missing_return: error
    dead_code: error
    invalid_annotation_target: error
    todo: ignore                      # allow TODO comments in source

  exclude:
    - "**/*.g.dart"                   # generated files
    - "**/*.freezed.dart"
    - example/**                      # don't lint the example app as strictly

linter:
  rules:

    # ─── Documentation ──────────────────────────────────────────
    # Critical for a public package — all public APIs must be documented
    public_member_api_docs: true
    comment_references: true
    slash_for_doc_comments: true

    # ─── Dart 3 / Null Safety ────────────────────────────────────
    cast_nullable_to_non_nullable: true
    null_closures: true
    prefer_null_aware_operators: true
    unnecessary_nullable_for_final_variable_declarations: true
    avoid_null_checks_in_equality_operators: true

    # ─── Code Style ──────────────────────────────────────────────
    prefer_single_quotes: true
    prefer_const_constructors: true
    prefer_const_declarations: true
    prefer_const_literals_to_create_immutables: true
    prefer_final_fields: true
    prefer_final_locals: true
    prefer_final_in_for_each: true
    unnecessary_this: true
    unnecessary_new: true
    unnecessary_const: true
    unnecessary_string_interpolations: true
    unnecessary_parenthesis: true
    unnecessary_raw_strings: true
    avoid_redundant_argument_values: true

    # ─── Error Prevention ────────────────────────────────────────
    avoid_catching_errors: true
    avoid_dynamic_calls: true
    avoid_empty_else: true
    avoid_print: true                 # use debugPrint in Flutter
    avoid_returning_null_for_future: true
    avoid_slow_async_io: true
    avoid_type_to_string: true
    cancel_subscriptions: true
    close_sinks: true
    literal_only_boolean_expressions: true
    no_adjacent_strings_in_list: true
    test_types_in_equals: true
    throw_in_finally: true
    unnecessary_statements: true
    always_declare_return_types: true

    # ─── Flutter Specific ────────────────────────────────────────
    use_key_in_widget_constructors: true
    sized_box_for_whitespace: true
    sized_box_shrink_expand: true
    avoid_unnecessary_containers: true
    use_colored_box: true
    use_decorated_box: true
    prefer_const_constructors_in_immutables: true

    # ─── Pub Package Quality ─────────────────────────────────────
    # These directly affect your pub.dev score
    secure_pubspec_urls: true
    sort_pub_dependencies: true

    # ─── Performance ─────────────────────────────────────────────

    prefer_for_elements_to_map_fromIterable: true
    prefer_spread_collections: true
    prefer_if_null_operators: true

    # ─── Naming & Structure ──────────────────────────────────────
    camel_case_types: true
    camel_case_extensions: true
    file_names: true
    non_constant_identifier_names: true
    constant_identifier_names: true
    library_names: true
    library_prefixes: true
    package_prefixed_library_names: true

    # ─── Explicitly Disabled ─────────────────────────────────────
    # Turned off intentionally — explain why for team clarity
    lines_longer_than_80_chars: false     # 80 is too strict for Flutter widget trees
    always_put_required_named_parameters_first: false  # conflicts with API compat goal
    sort_constructors_first: false        # personal preference, not enforced
