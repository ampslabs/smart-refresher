name: smart_refresher
description: A maintained community fork of flutter_pulltorefresh for pull-to-refresh and infinite loading in Flutter scrollables.
version: 0.1.0
homepage: https://github.com/your-github-username/smart-refresher

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
